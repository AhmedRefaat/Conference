<!DOCTYPE html>
<html>
<head>
<style>
a:link {color:black;}    /* unvisited link */
a:visited {color:black;} /* visited link */
a:hover {color:brown;}   /* mouse over link */
a:active {color:#00F0FF;}  /* selected link */
a {text-decoration: none}
a:hover img {
border: none !important;
display: block;
}
</style>
</head>
<div id="main">

<div id = "centered">
<p>
	<% if @new_submit != 0%>
<h1 style="text-align: center">List of Paper You have submited</h1>
<!-- <h2 > Submission </h2> --> 
<table>
  <tr>
    <th>Title</th>
    <th>Abstract</th>
    <th>Auth1</th>
    <th>Paper Approval</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @papers.each do |paper| %>
  <tr>
    <td><%= paper.title %></td>
    <td><%= paper.abstract %></td>
    <td><%= paper.auth1 %></td> 
    <% if paper.acceptance == nil %>
    <td style="text-align: center; color: red">Pending</td>
    <% elsif paper.acceptance %>
    <td style="text-align: center; color: green">Accepted</td>
    <% elsif !paper.acceptance %>
    <td style="text-align: center; color: red">Refused</td>
    <% end %>
    <td><%= link_to 'Show', paper %></td>
    <td><%= link_to 'Edit', edit_paper_path(paper) %></td>
<!--    <td><%= link_to 'Destroy', paper, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
  </tr>
<% end %>
</table>

<br />
<!--
<%=  link_to 'New Paper', new_paper_path %>
--> 
</p>

<%else %>

<h3 style="text-align: center;color: black "><font size="5" face="Garamond" color="red"> Sorry There is no submission available any more!! </font></h1>
	<%end%>
</div>
</div>
